import { Box } from "@mui/material";
import ITorrent from "../interfaces/ITorrent";

interface SelectedTorrentProps {
  torrent: ITorrent;
}

const SelectedTorrent = (props: SelectedTorrentProps) => {
  return <Box></Box>;
};

export default SelectedTorrent;
